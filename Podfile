# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'InstantNotification' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'SwiftLint'

  # Pods for InstantNotification

  target 'InstantNotificationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'InstantNotificationUITests' do
    # Pods for testing
  end

end
